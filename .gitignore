# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Personal recipe intake photos (phone screenshots) — never publish to the
# now-public repo; only the extracted recipe JSON gets committed.
Recipes-intake/

# Design review packs prepared for external reviewers (Fable/cowork) — never
# publish; regenerate on demand instead of tracking in git.
design-review-pack/
design-review-pack.zip
